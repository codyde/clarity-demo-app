# Clarity Demo Application 

This application is a simple ClarityUI Driven Demo application using the following elements 

* ClarityUI header
* Vertical Nav
* Datagrid 

This application uses a static chunk of JSON data to populate the data grid object. 